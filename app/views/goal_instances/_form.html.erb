<%= form_for(@goal_instance) do |f| %>
  <% if @goal_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_instance.errors.count, "error") %> prohibited this goal_instance from being saved:</h2>

      <ul>
      <% @goal_instance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date %>
  </div>
  <div class="actions">
    <%= f.hidden_field :is_complete %>
    <%= f.hidden_field :cheer_ons %>
    <%= hidden_field_tag ':is_complete' 'false' %>
    <%= hidden_field_tag ':cheer_ons' '0' %>
    <%= f.submit %>
  </div>
<% end %>
